#include <stdio.h>

int main(){
  
    /* arithmetic operators:
 
    +  (addition)
    -  (subtraction)
    *  (multiplication)
    /  (division)
    %  (modulus)
    ++ (ncrement)
    -- (decrement) 
    
    */
 
    int x = 5;
    int y = 2;
    
    // int z = x + y;
    // int z = x - y;
    // int z = x * y;
    // float z = x / (float) y;
    // int z = x % y;
    // x++;
    // y--;

    printf("\n%d\n", x);
    printf("\n%d\n", y);
    

    return 0;
}